<%= FormPresenter.new(f, self).notes %>
<fieldset id="Register-fields">
  <legend><%= @acc_name %>（<%= @nickname %>）様の登録チェック・今は<%= @month %> 月です。</legend>
  <%= render 'register_fields', f: f, confirming: false %>
</fieldset>