<% @title = '年度別成績検索' %>
<h1><%= @title %></h1>

<div id="generic-form">
  <%= form_for @year_form, as: 'form', url: [ :admin, @year_form.year ] do |f| %>
    <%= render 'form', f: f %>
    <div class="buttons">
      <%= f.submit '検索', class: "btn btn-default", data: {disable_with: '検索中・・・' } %>
      <%= link_to 'キャンセル', :admin_root %>
    </div>
  <% end %>
</div>
