<% @title = 'アカウント情報編集' %>
<h1><%= @title %></h1>

<div id="generic-form">
  <%= form_for @staff_member, url: :confirm_admin_account do |f| %>
    <%= render 'form', f: f %>
    <div class="buttons">
      <%= f.submit '確認画面へ進む' %>
      <%= link_to 'キャンセル', :admin_root%>
    </div>
  <% end %>
</div>