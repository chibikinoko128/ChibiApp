<% @title = '現在のクオーターのボーナス獲得者一覧　（途中経過）' %>
<h1><%= @title %></h1>

<% if @bonus_member.present? %>
  <div class="table-wrapper">
    <p><%= link_to '月別キャッシュバック獲得者一覧', :cache_acquisition_admin_record %><br>

    <%= link_to '特別会員ボーナス獲得者一覧', :sp_member_admin_record %><br>

    <%= link_to 'ボーナス獲得者一覧', :bonus_acquisition_admin_record %></p>
    <h2><strong>現在は<%= @year %>年　第<%= @quarter %>クオーターの途中です。</strong></h2>
    <table class="attributes">
      <tr><th colspan="3"><strong><%= @year %>年　第<%= @quarter %>クオーター　</strong>支払い総額　￥<%= number_with_delimiter(@total_bonus_pay) %></th></tr>
    </table>

    <%= paginate @bonus_member %>

    <%= render 'check_bonus' %>

    <%= paginate @bonus_member %>
  </div>
<% else %>
  <h2><strong>ボーナス獲得者はおりません。</strong></h2>
<% end %>
    
<div class="table-wrapper">
  <div class="links">
    <%= link_to '前ページへ戻る', :acquisition_member_admin_record %>
  </div>
  <div class="links">
    <%= link_to '検索ページへ', :edit_admin_pay %>
  </div>
  <div class="links">
    <%= link_to 'トップ画面へ戻る', :admin_root %>
  </div>
</div>
