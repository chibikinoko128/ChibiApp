<% @title = '年度別成績検索' %>
<h1><%= @title %></h1>

<div id="generic-form">
  <%= form_for @customer_year_form, as: 'form', url: [ :customer, @customer_year_form.customer_year ] do |f| %>
    <%= render 'form', f: f %>
    <div class="buttons">
      <%= f.submit '検索', class: "btn btn-default", data: {disable_with: '検索中・・・' } %>
      <%= link_to 'キャンセル', :customer_root %>
    </div>
  <% end %>
</div>
