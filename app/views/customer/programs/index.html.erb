<% @title = 'プログラム一覧' %>
<h1><%= @title %></h1>

<div class="table-wrapper">
   <p>サイトのイベントへの参加申し込みは「詳細」をクリックし「申し込む」ボタンをクリックしてください。</p>
  <%= paginate @programs %>

  <table class="listing">
    <tr>
      <th>タイトル</th>
      <th>申し込み開始日時</th>
      <th>申し込み終了日時</th>
      <th>最小参加者数</th>
      <th>最大参加者数</th>
      <th>アクション</th>
    </tr>
    <%= render partial: 'program', collection: @programs %>
  </table>

  <%= paginate @programs %>
</div>

<div class="table-wrapper">
  <div class="links">
    <%= link_to 'トップ画面へ戻る', :customer_root %>
  </div>
</div>

